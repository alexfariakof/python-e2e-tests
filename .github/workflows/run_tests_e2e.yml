on:
  push:
    branches: [ main ]
  
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
    
jobs:
  Run-Tests-e2e:
    runs-on: ubuntu-latest
    name: Job Run Playwright Tests    
    steps:      
      - name: Run Playwright Tests        
        id: tests
        uses: alexfariakof/despesas-frontend-angular-tests-e2e@main        